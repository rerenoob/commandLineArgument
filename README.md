# commandLineArgument
Sample project that uses the NDesk command line library.

## Requirement
This project is run with dotnet core.

## Build
To build this project, use:
`dotnet run [--PARAMETER]`

* NOTE: to use `dotnet run` along with the command line arguments, use the double-dash prefix for the paramter. 
